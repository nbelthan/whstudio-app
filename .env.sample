# Database Configuration - Neon PostgreSQL
DATABASE_URL="postgresql://username:password@host/database?sslmode=require"
DATABASE_URL_NON_POOLING="postgresql://username:password@host/database?sslmode=require"

# Alternative Postgres Environment Variables (for Vercel compatibility)
POSTGRES_URL="postgresql://username:password@host/database?sslmode=require"
POSTGRES_URL_NON_POOLING="postgresql://username:password@host/database?sslmode=require"
POSTGRES_USER="username"
POSTGRES_HOST="hostname"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database_name"

# World ID Configuration
NEXT_PUBLIC_APP_ID="app_1234567890abcdef" # Replace with your actual World App ID from developer portal
NEXT_PUBLIC_WLD_APP_ID="app_1234567890abcdef" # Same as above for client-side usage
WORLD_APP_ID="app_1234567890abcdef" # Same as above for server-side usage
WORLD_ACTION_ID="verify_human" # Default action for World ID verification
WORLD_API_KEY="" # Optional API key for enhanced World ID features
WORLD_API_BASE_URL="https://developer.worldcoin.org/api/v1" # World ID API endpoint

# Authentication & Session Management
AUTH_SECRET="" # Generate with: npx auth secret or openssl rand -base64 32
HMAC_SECRET_KEY="" # Generate with: openssl rand -base64 32
JWT_SECRET="" # Generate with: openssl rand -base64 32
AUTH_URL="http://localhost:3000" # For development. Use production URL for deployment

# Vercel KV (Redis) for Session Storage
KV_URL="redis://default:token@endpoint.kv.vercel-storage.com:6379"
KV_REST_API_URL="https://endpoint.kv.vercel-storage.com"
KV_REST_API_TOKEN="your_kv_rest_token"
KV_REST_API_READ_ONLY_TOKEN="your_kv_read_only_token"
REDIS_URL="redis://default:token@endpoint:port" # Alternative Redis configuration

# Vercel Blob Storage for File Uploads
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_your_token_here" # From Vercel dashboard

# Payment Configuration
PAYMENT_WEBHOOK_SECRET="" # Generate with: openssl rand -base64 32

# Environment Settings
NODE_ENV="development" # development | production
NEXT_PUBLIC_API_URL="http://localhost:3000" # Base URL for API calls
NEXT_PUBLIC_DEVELOPMENT_MODE="true" # Enable development features

# Demo Mode Settings
DEMO_REWARD="0.02" # Reward amount per task in demo mode (in USDC/WLD)
DEMO_CURRENCY="USDC" # Currency for demo mode (USDC or WLD)